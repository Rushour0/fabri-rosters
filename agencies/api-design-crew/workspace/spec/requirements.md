# Saved searches

Add saved searches to the product. A signed-in user can create, list, rename, run, and delete their own saved searches. Each saved search has a name, query text, and optional filters. Search results should remain paginated.
