# Copy-Me Agency

Copy this directory to `agencies/<name>/`, rename the manifest values, and tailor the manager, specialist, and workspace to one clear deliverable.

Keep `__AGENCY_ROOT__`, `__AGENCY_SLUG__`, and `__RUN_FROM__` in configuration paths and memory collections: fabri replaces them when installing the agency.
