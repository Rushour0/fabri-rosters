[agency]
name = "deployable-html-verify-crew"
title = "Deployable HTML Verify Crew"
tagline = "The deployability gate that boot-smokes a self-contained HTML playable ad against the engines/web frozen contract and turns failures into concrete repairs."
category = "Games & Interactive"
deliverable = "A recorded pass/fail verdict (verify.json) on build/<slug>/index.html plus, on failure, ordered repair instructions for the builder."
entry = "agent.openai.yaml"

[agency.stats]
agents = 2
tools = 4

[agency.cogs]
max_cost_usd = 0.20
provider = "openai"

[agency.wedge]
self_improving = true
cogs_reported = true
